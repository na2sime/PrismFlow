{
  "name": "prismflow",
  "version": "1.0.0",
  "description": "Self-hosted project management application",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd app/server && npm run dev",
    "client:dev": "cd app/client && npm start",
    "build": "cd app/client && npm run build",
    "start": "cd app/server && npm start",
    "install:all": "npm install && cd app/client && npm install && cd ../server && npm install"
  },
  "keywords": ["project-management", "react", "express", "sqlite", "glassmorphism"],
  "author": "ABDIOU Nassime",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}